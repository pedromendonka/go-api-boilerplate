# queries

This folder contains SQL query files used by `sqlc` to generate type-safe Go code for database access. Each `.sql` file defines one or more queries or commands that are mapped to Go methods.

- **Purpose:** Store SQL queries and commands for code generation.
- **Generated:** No, these are written and maintained manually. The Go code generated from these files is placed in the `db` folder.

